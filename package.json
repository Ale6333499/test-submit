{
  "scripts": {
    "build": "vite build",
    "build-zip": "rm -r dist && rm dist.zip && vite build && zip -r dist.zip dist && git add -f dist.zip && git commit -m 'update dist.zip' && git push",
    "dev": "vite"
  },
  "dependencies": {
    "@unocss/reset": "^0.57.7",
    "@vueuse/core": "^8.2.0",
    "primeicons": "^6.0.1",
    "primevue": "^3.41.1",
    "vue": "^3.2.31"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.2.4",
    "@vue/compiler-sfc": "^3.2.31",
    "typescript": "^4.6.3",
    "unocss": "^0.57.7",
    "vite": "^2.9.16"
  }
}
